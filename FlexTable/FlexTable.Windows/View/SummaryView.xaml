<UserControl
    x:Class="FlexTable.View.SummaryView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FlexTable.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d3="using:d3.View"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">
    
    <UserControl.Resources>
        <Style TargetType="TextBlock" x:Key="DescriptionStyle">
            <Setter Property="FontSize" Value="20" />
            <Setter Property="TextAlignment" Value="Right" />
            <Setter Property="Padding" Value="0 5 10 5" />
        </Style>
    </UserControl.Resources>
    
    <Grid Margin="10">
        <StackPanel>
            <TextBlock Style="{StaticResource DescriptionStyle}" FontSize="30" FontWeight="Bold" Text="{Binding Column.Name}" />
            <TextBlock Style="{StaticResource DescriptionStyle}" Text="{Binding Column.TypeString}" />
        </StackPanel>
        <Canvas>
            <d3:Rectangles Data="{Binding Data}" WidthGetter="{Binding WidthGetter}" HeightGetter="{Binding HeightGetter}" 
                           XGetter="{Binding XGetter}" YGetter="{Binding YGetter}"/>
            <d3:Axis Orientation="Horizontal" Scale="{Binding XScale}" Canvas.Top="400" Canvas.Left="0"/>
            <d3:Axis Orientation="Vertical" Scale="{Binding YScale}" Canvas.Top="0" Canvas.Left="50"/>
        </Canvas>
    </Grid>
</UserControl>
