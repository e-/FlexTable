<UserControl
    x:Class="FlexTable.View.PageView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FlexTable.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d3view="using:d3.View"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <UserControl.Resources>
        <Style TargetType="TextBlock" x:Name="TitleStyle">
            <Setter Property="FontSize" Value="42" />
            <Setter Property="TextAlignment" Value="Center"></Setter>    
        </Style>
        <Style TargetType="TextBlock" x:Name="SubtitleStyle">
            <Setter Property="FontSize" Value="18" />
            <Setter Property="TextAlignment" Value="Center"></Setter>
        </Style>
    </UserControl.Resources>
    
    <StackPanel Margin="20">
        <StackPanel Height="480">
            <TextBlock Style="{StaticResource TitleStyle}">
                <Run Text="{Binding ColumnViewModel.Column.Name}" />
            </TextBlock>
            <TextBlock Style="{StaticResource SubtitleStyle}">
                <Run Text="{Binding ColumnViewModel.TypeString}" />
            </TextBlock>
            
            <local:NumericalDescriptiveStatisticsView DataContext="{Binding BoxPlotViewModel}"></local:NumericalDescriptiveStatisticsView>

            <d3view:BoxPlot Margin="0 50 0 0" x:Name="BoxPlotElement" Min="{Binding BoxPlotViewModel.Min}" Max="{Binding BoxPlotViewModel.Max}"
                                FirstQuartile="{Binding BoxPlotViewModel.FirstQuartile}" ThirdQuartile="{Binding BoxPlotViewModel.ThirdQuartile}"
                                Mean="{Binding BoxPlotViewModel.Mean}" Median="{Binding BoxPlotViewModel.Median}" Visibility="Collapsed"
                                ></d3view:BoxPlot>
            <d3view:BarChart x:Name="BarChartElement" Visibility="Collapsed"></d3view:BarChart>
        </StackPanel>
        <Image Source="ms-appx:///Assets/horizontal-separator.png" Width="700" Height="8"></Image>
    </StackPanel>
</UserControl>
