<UserControl
    x:Class="FlexTable.View.DistributionView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FlexTable.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d3view="using:d3.View"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <StackPanel>
        <d3view:BarChart x:Name="HistogramElement" HorizontalAxisVisibility="Collapsed" LegendVisibility="Collapsed"
                                         Width="670" Height="265" AutoColor="False"
                                         ></d3view:BarChart>

        <d3view:BoxPlot Margin="50 0 0 0" x:Name="BoxPlotElement" Min="{Binding Min}" Max="{Binding Max}"
                                    FirstQuartile="{Binding FirstQuartile}" ThirdQuartile="{Binding ThirdQuartile}"
                                    Mean="{Binding Mean}" Median="{Binding Median}" Width="620"
                                    ></d3view:BoxPlot>
    </StackPanel>
</UserControl>
