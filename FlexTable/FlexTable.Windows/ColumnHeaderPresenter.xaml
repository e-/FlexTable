<UserControl
    x:Class="FlexTable.ColumnHeaderPresenter"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FlexTable"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <ScrollViewer VerticalScrollBarVisibility="Hidden" HorizontalScrollBarVisibility="Hidden" 
                  HorizontalScrollMode="Enabled" 
                  Width="{Binding SheetViewWidth}" Name="ColumnHeaderScrollViewer">
        <Grid Height="{StaticResource ColumnHeaderHeight}" Width="{Binding SheetHeight}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="40"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>

            <Canvas x:Name="CellCanvas" Grid.Column="1" Background="#EEEEEE"></Canvas>
        </Grid>
    </ScrollViewer>
</UserControl>
