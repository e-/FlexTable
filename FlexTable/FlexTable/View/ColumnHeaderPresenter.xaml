<UserControl
    x:Class="FlexTable.View.ColumnHeaderPresenter"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FlexTable.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    >

    <Border Background="{StaticResource GridLineBrush}" Width="{Binding SheetViewWidth}">
        <ScrollViewer VerticalScrollBarVisibility="Hidden" HorizontalScrollBarVisibility="Hidden"
                      HorizontalScrollMode="Disabled" VerticalScrollMode="Disabled" Name="HeaderScrollViewer"
                      ZoomMode="Disabled"
                      Width="{Binding SheetViewWidth}"
                      >
            <Canvas x:Name="Items" Width="{Binding PaddedSheetWidth}"></Canvas>
        </ScrollViewer>
    </Border>
</UserControl>
