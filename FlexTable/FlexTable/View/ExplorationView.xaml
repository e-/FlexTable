<UserControl
    x:Class="FlexTable.View.ExplorationView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FlexTable.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d3view="using:d3.View"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid x:Name="Wrapper">
        <Canvas x:Name="PageViewsElement">
            <Border Canvas.Top="{Binding PageHeight}" Width="{Binding PageWidth}">
                <local:MetedataView DataContext="{Binding MetadataViewModel}"></local:MetedataView>
            </Border>
            
            <local:PageView x:Name="InitialPageViewElement" DataContext="{Binding DummyPageViewModel}" Width="{Binding PageWidth}"></local:PageView>
        </Canvas>
        <Grid x:Name="StrokeGrid" IsHitTestVisible="False"></Grid>
        <Grid x:Name="NewStrokeGrid" IsHitTestVisible="False"></Grid>
    </Grid>
</UserControl>
